import React from "react";

const Quantity = () => {
  return (
    <div>
      {/* <div className="space-y-3">
        <label className="text-sm font-bold text-brand-gray-900/60 ml-1">
          Weeks Duration
        </label>
        <div className="flex items-center gap-4 bg-brand-gray-50 p-1.5 rounded-2xl border border-brand-gray-100">
          <button
            onClick={() => setWeeksCount(Math.max(1, weeksCount - 1))}
            className="w-12 h-12 rounded-xl bg-white shadow-sm hover:bg-brand-gray-50 flex items-center justify-center font-black text-brand-orange transition-all"
          >
            -
          </button>
          <span className="flex-1 text-center font-bold text-lg">
            {weeksCount} Weeks
          </span>
          <button
            onClick={() => setWeeksCount(weeksCount + 1)}
            className="w-12 h-12 rounded-xl bg-white shadow-sm hover:bg-brand-gray-50 flex items-center justify-center font-black text-brand-orange transition-all"
          >
            +
          </button>
        </div>
      </div> */}
    </div>
  );
};

export default Quantity;
